[package]
name = "learn-rocket"
version = "0.1.0"
edition = "2021"

[dependencies]
rocket = { version = "0.5", features = ["json"] }
#serde = { version = "1", features = ["derive"] }
#jsonwebtoken = "9"
#rocket-jwt = "0.5"

#permit-micro = { path = "permit-micro" }

